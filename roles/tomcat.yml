---
- name: Tomcat deployment playbook
  hosts: localhost
  command: ps aux
  become: true
  become_method: su
  become_user: nobody
  become_flags: "-s /bin/bash"
  gather_facts: true
  remote_user: ubuntu
  roles:
    - tomcat
